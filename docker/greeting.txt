Installation complete!

1. Run `composer wp-config-update` to load the values from the .env file and overwrite the old one. Please make a backup of wp-config.php first! 
   When this file is found in public/, it will be moved to the root level.

2. Start the application with `docker-compose up -d`
